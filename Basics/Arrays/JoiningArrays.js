const numbers = [1,2,3]
const joined = numbers.join(', ') // joins an array into one string with a sequence
console.log(joined); // print 1, 2, 3

const message = 'This is my first message'
const parts = message.split(' ')
console.log(parts);

const combined = parts.join("|")
console.log(combined)
